{
  "name": "@saus/repng",
  "version": "0.1.0",
  "description": "React-driven PNG renderer for Saus",
  "license": "MIT",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "build": "rimraf dist && tsup",
    "dev": "tsup --watch",
    "prepare": "yarn build"
  },
  "peerDependencies": {
    "react": "*",
    "react-dom": "*",
    "saus": "*"
  },
  "devDependencies": {
    "@types/react": "^17.0.25",
    "@types/react-dom": "^17.0.9",
    "saus": "*"
  },
  "dependencies": {
    "@saus/react": "*",
    "@saus/repng": "link:",
    "repng": "link:vendor/repng"
  }
}
